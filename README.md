This project is powered with [Jekyll](http://jekyllrb.com/). Visit [wallmanderco.github.io](http://wallmanderco.github.io) to actually see the site.

### Install
```bash
gem install jekyll
gem install bundler
rbenv rehash
bundle install
```

### Run
```
bundle exec jekyll serve --watch --drafts
```